{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "WasherDryerBackend (Tomcat)",
      "request": "launch",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
